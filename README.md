# Google-sheet-Backup-Management-system-
This repo contains app script for google sheets backup system. This system automatically (based on set interval)backs up the required sheet in high fidelity.It allows easy restore option with retaining all the formats and template as per original sheet.
